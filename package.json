{
  "name": "video-multiplier-ai",
  "version": "1.0.0",
  "private": true,
  "description": "Multiplicador de vídeos para TikTok Shop: gancho + desenvolvimento + CTA.",
  "type": "module",
  "scripts": {
    "start": "node server.js",
    "dev": "node --watch server.js",
    "check": "node --check server.js && node --check lib/media.js && node --check public/app.js"
  },
  "engines": {
    "node": ">=20"
  },
  "dependencies": {
    "archiver": "^7.0.1",
    "express": "^5.1.0",
    "ffmpeg-static": "^5.2.0",
    "ffprobe-static": "^3.1.0",
    "multer": "^2.0.2"
  }
}
